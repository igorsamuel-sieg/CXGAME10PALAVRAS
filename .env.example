# Copie este arquivo para .env e configure as variáveis
# No Render, configure estas variáveis em Environment > Environment Variables

# Senha para acesso ao painel do Mestre
MASTER_PASSWORD=hogwarts2024

# Senha para acesso como Espectador (opcional, deixe vazio para acesso livre)
SPECTATOR_PASSWORD=

# Porta do servidor (o Render define automaticamente via PORT)
PORT=3000
